# Todos

- Add `inventory/` structure and documentation
- Add templates for `item.yaml` and `listing.yaml`
- Keep compatibility with existing `.signals/` batch tracking (no changes required)
- Run `coderabbit --prompt-only`
